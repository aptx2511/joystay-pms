import { NextResponse } from "next/server";
// Full implementation comes in Step 4 (iCal sync)
// This stub allows the Settings page to function without errors

export async function POST() {
  return NextResponse.json({
    message: "iCal sync will be implemented in Step 4. Save your Airbnb URLs first.",
  });
}
